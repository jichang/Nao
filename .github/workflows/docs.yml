name: Deploy Docs to GitHub Pages

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - README.md
      - docs/**
      - scripts/validate-docs.py
      - dotnet-tools.json
      - .github/workflows/docs.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore tools
        run: dotnet tool restore

      - name: Restore dependencies
        run: dotnet paket restore

      - name: Build
        run: dotnet build Nao.slnx -c Release

      - name: Validate documentation sources
        run: python3 scripts/validate-docs.py

      - name: Generate docs
        run: dotnet fsdocs build --clean --output ./site --properties Configuration=Release --parameters root /Nao/

      - name: Validate generated documentation
        run: python3 scripts/validate-docs.py --site ./site

      - name: Add .nojekyll
        if: github.event_name != 'pull_request'
        run: touch ./site/.nojekyll

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./site
          include-hidden-files: true

  deploy:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
